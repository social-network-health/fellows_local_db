# Dev/test stack: pytest, Playwright, and pytest-playwright (e2e).
# After pip install, run: playwright install chromium  (see README)
pytest>=9.1
playwright>=1.61
pytest-playwright>=0.8

# Icon regeneration (build/generate_icons.py). cairosvg also needs the
# native cairo library — on macOS: `brew install cairo`.
cairosvg>=2.9
pillow>=12.3

# Bundle signing (scripts/sign_bundle.py + scripts/keygen_signing_key.py).
# Also used by the dev server to sign manifests on the fly so e2e tests
# exercise the same SW signature-verify path as production. The prod
# server never imports this — it only serves the static manifest.sig
# that the maintainer rsyncs up.
cryptography>=49.0
